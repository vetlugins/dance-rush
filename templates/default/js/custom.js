$(document).ready(function () {

    /* Inputmask
     ---------------------------------------------------------------------- */
    $(":input").inputmask();

    /* Tooltip
     ---------------------------------------------------------------------- */
    $('[data-toggle="tooltip"]').tooltip()


});
